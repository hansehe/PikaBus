# general things to ignore
build/
dist/
*.egg-info/
*.egg
*.py[cod]
__pycache__/
*.so
*~

# due to using tox and pytest
.tox
.cache

.vscode/
.idea/
venv/
# Never copy a host virtualenv into the image - it would shadow the one uv builds inside.
.venv/
_build/
docker-compose.build.*.yml
docker-compose.publish.*.yml
docker-compose.test.*.yml
docker-compose.run.*.yml
docker-compose.digest*.yml

.git/
